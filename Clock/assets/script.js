console.log("hello!")




// loads MotionPathPlugin into gsap
gsap.registerPlugin(MotionPathPlugin);


// animate:
// 86400
var first = gsap.to("#rect", {
  duration: 40, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#first_path",
    align: "#first_path",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()



gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle2", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#second_path",
    align: "#second_path",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()





gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle3", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#third_path",
    align: "#third_path",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle4", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#fourth_path",
    align: "#fourth_path",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle5", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#fifth_path",
    align: "#fifth_path",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle6", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#sixth_path",
    align: "#sixth_path",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle7", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#seventh_path",
    align: "#seventh_path",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle8", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#eighth",
    align: "#eighth",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle9", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#nineth",
    align: "#nineth",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()

gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle10", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#tenth",
    align: "#tenth",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle11", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#eleventh",
    align: "#eleventh",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle12", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#twelve",
    align: "#twelve",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()

gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle13", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#thirteen",
    align: "#thirteen",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()

gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle14", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#fourteen",
    align: "#fourteen",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle15", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#fifteen",
    align: "#fifteen",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle16", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#sixteen",
    align: "#sixteen",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()


gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle17", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#seventeen",
    align: "#seventeen",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()

gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle18", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#eighteen",
    align: "#eighteen",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()

gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle19", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#nineteen",
    align: "#nineteen",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()

gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle20", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#twenty",
    align: "#twenty",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()

gsap.registerPlugin(MotionPathPlugin);


var second = gsap.to("#circle21", {
  duration: 86400, 
  repeat: 2,
  ease: "power1.inOut",
  motionPath:{
    path: "#twentyone",
    align: "#twentyone",
    autoRotate: true,
    alignOrigin: [0.5, 0.5]
  }
});
first.pause()


// date object to get current time
// convert current time into total seconds
// set seconds to seek:
first.seek(2)
first.play()





//how do i make the circle show up fully along the line since it keeps getting cut off
//How do I move the circles with the time? 

